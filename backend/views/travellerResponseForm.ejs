<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Respond to <%= post.productName || "Post" %> | SendMate</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 font-sans ">
    <div class="wrapper min-h-screen flex flex-col">
        <header class="bg-white shadow fixed top-0 w-full z-10">
            <% if (!user) { %>
                <%- include('partials/nav') %> <!-- Nav for guests -->
                    <% } else { %>
                        <%- include('partials/nav_users') %> <!-- Nav for logged-in users -->
                            <% } %>
        </header>
        <main class="flex-grow mt-12">
            <div class="max-w-3xl mx-auto mt-10 p-8 bg-white rounded-lg shadow-lg">
                <h2 class="text-2xl font-bold text-center text-blue-700 mb-6">
                    Respond to "<%= post.productName || postType %>"
                </h2>

                <form action="/submitResponse/<%= post.postType %>/<%= post._id %>" method="POST" class="space-y-6">
                    <div>
                        <label for="message" class="block text-sm font-semibold text-gray-700">Message to Sender</label>
                        <textarea id="message" name="message" rows="4" required
                            class="w-full p-3 mt-1 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400"></textarea>
                    </div>

                    <div>
                        <label for="estimatedDelivery" class="block text-sm font-semibold text-gray-700">Estimated
                            Delivery
                            Date</label>
                        <input type="date" id="estimatedDelivery" name="estimatedDelivery" required
                            class="w-full p-2 mt-1 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400">
                    </div>

                    <div>
                        <label for="priceOffer" class="block text-sm font-semibold text-gray-700">Your Price Offer (in
                            ₹)</label>
                        <input type="number" id="priceOffer" name="priceOffer" required min="0"
                            class="w-full p-2 mt-1 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400">
                    </div>

                    <div class="text-center">
                        <button type="submit"
                            class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-6 rounded-lg shadow-md transition duration-300">
                            Submit Response
                        </button>
                    </div>
                </form>
            </div>
        </main>
        <!-- Footer -->
        <%- include('partials/footer') %>

    </div>
</body>

</html>