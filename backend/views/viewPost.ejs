<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= post.productName %> | SendMate
    </title>
    <link rel="stylesheet" href="/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="wrapper">
        <header>
            <%- include('partials/nav_users') %>
        </header>

        <main>
            <div class="container mx-auto bg-white mt-10 p-8 rounded-lg shadow-lg max-w-4xl w-full">
                <h2 class="text-2xl font-bold text-center mb-6 text-gray-800">
                    <%= post.productName %>
                </h2>

                <% if (post.imageUrl) { %>
                    <div class="flex justify-center mb-6">
                        <img src="<%= post.imageUrl %>" alt="Post Image"
                            class="rounded-lg shadow-md max-h-64 object-cover">
                    </div>
                    <% } %>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 text-gray-700">
                            <div><strong>Source:</strong>
                                <%= post.source %>
                            </div>
                            <div><strong>Destination:</strong>
                                <%= post.destination %>
                            </div>
                            <div><strong>Expected Delivery:</strong>
                                <%= new Date(post.expectedTime).toLocaleDateString('en-IN', { weekday: 'long' ,
                                    year: 'numeric' , month: 'long' , day: 'numeric' }) %>
                            </div>
                            <div><strong>Payment Range:</strong> ₹<%= post.paymentMin %> - ₹<%= post.paymentMax %>
                            </div>
                            <div><strong>Category:</strong>
                                <%= post.category %>
                            </div>
                            <div><strong>Product Weight:</strong>
                                <%= post.productWeight %> kg
                            </div>
                            <div><strong>Dimensions (L×B×H):</strong>
                                <%= post.length %> x <%= post.breadth %> x <%= post.height %> cm
                            </div>
                            <div><strong>Fragile:</strong>
                                <%= post.isFragile %>
                            </div>
                            <div><strong>Receiver Details:</strong>
                                <%= post.receiverDetails %>
                            </div>
                            <div class="md:col-span-2"><strong>Additional Info:</strong>
                                <%= post.additionalDetails || 'N/A' %>
                            </div>
                            
                            
                        </div>
                        <div class="flex justify-center mt-8">
                            <a href="/responseForm/<%=post.postType%>/<%= post._id %>"
                                class="bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-lg shadow-md transition duration-300">
                                Fill Response Form
                            </a>
                        </div>
                        <div class="md:col-span-2 text-right mt-5 text-sm text-gray-500">
                            Posted by <%= post.username %> (<%= post.email %>)
                        </div>
            </div>
        </main>

        <footer class="mt-10">
            <%- include('partials/footer') %>
        </footer>
    </div>
</body>

</html>